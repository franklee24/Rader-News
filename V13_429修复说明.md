# 雷达新闻 V13：GDELT 429 修复版

本版针对 GitHub Actions 中出现的：
- HTTP 429
- JSONDecodeError（上游限流/非 JSON 响应）
- 31 个国家逐国请求导致请求过密

做了结构性修改。

## 核心变化

1. 不再“每个国家请求一次”。
2. 改为 Tier 1 / Tier 2 / Tier 3 / Tier 4 各自 1 次公共数据池请求，另加 1 次全球补充。
3. 总请求量从原来的约 32 次降到 5 次。
4. 请求之间增加 12 秒间隔。
5. 429 使用指数退避，最长单次等待约 120 秒。
6. JSONDecodeError / 超时 / 5xx 也会重试。
7. GitHub Actions 时间改为北京时间每天 08:07，避开整点高负载窗口。
8. 某一梯队请求失败时，自动使用上一份成功快照，不会把整个网站清空。
9. 仍保留“宁缺毋滥”和每日 24 小时窗口。
10. 仍保留 Tier 1=20–30、Tier 2≤20、Tier 3≤15、Tier 4≤10、全球补充≤10 的上限。

## 注意

首次运行仍取决于 GDELT 当前是否对 GitHub Actions 出口 IP 限流。如果 GDELT 对该出口继续返回 429，本版会自动重试并最终回退到上一份快照；这不是代码异常，而是上游限流。

如果连续多次 429，下一阶段应把采集源从“单一 GDELT”升级成多源采集（RSS/官方源/Reuters/AP/BBC 等）并做统一事件聚类，这会比继续堆 GDELT 重试更稳。
