# 雷达新闻 V16：只需要做一次

1. 将本目录全部文件覆盖到 GitHub 仓库 `franklee24/Rader-News` 的 `main`。
2. **删除旧的 `Deploy 雷达新闻 Pages` workflow**（避免两个 Pages workflow 同时部署）。
3. 保留并覆盖 `.github/workflows/daily.yml`。
4. Settings → Pages → Source 保持 `GitHub Actions`。
5. Actions → `雷达新闻每日更新` → Run workflow → main。
6. 等待同一个运行中的 `Deploy to GitHub Pages` 变成绿色。
7. 打开 https://franklee24.github.io/Rader-News/ 并强制刷新。

以后不需要手动运行 Pages workflow。每日 08:00（北京时间）同一个 workflow 会完成：抓取 → 生成 → 校验 → 保存 → Pages 部署。
